# Active Claude Code config — populated by claude-code/install.sh from templates
.claude/

# Per-machine local settings overrides
.claude/settings.local.json

# Sprint state moved to docs/internal/sprint-state.md in
# oss-readiness/g5-root-cleanup. No longer relevant at root.

# Rust
target/
**/*.rs.bk
Cargo.lock.bak

# proptest minimal-failure cases (re-derived on every run; no value
# committing the local reproducer).
**/proptest-regressions/
**/*.proptest-regressions

# Editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets
.env
.env.local
*.pem
*.key
