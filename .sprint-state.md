# Sprint S3 state

**Branch:** `vm/projectors`
**Started:** 2026-05-08
**Exit gate:** 10k-iter property test asserting `EVM balanceOf == Move Coin.value` after arbitrary mixed-VM transaction sequences.

## Plan

S3 broken into four slices:

1. **VM-shape transaction types** — `EvmTx`, `MoveTx`, both reducing to `CanonicalTransfer` via `to_canonical()`.
2. **Read-side projectors** — `EvmProjector` / `MoveProjector` traits + `EvmView` / `MoveView` defaults.
3. **Mock executors** — `MockEvm` / `MockMove` in `gsxdb-bridge::vm::executor`, both routing through `Bridge::submit`.
4. **Cross-VM parity property tests** — 4 properties at 10k cases, the exit gate among them.

Plus IQ-2 + spec doc + sprint state.

## Completed

- [x] Slice 1+2 (commit `c7905dc`): `gsxdb-state::vm` module — `tx`
  submodule (EvmTx/MoveTx + CanonicalTransfer) and `projector` submodule
  (EvmProjector/MoveProjector traits + EvmView/MoveView defaults).
  - 7 unit tests across the two submodules.

- [x] Slice 3 (commit `9854f5f`): `gsxdb-bridge::vm::executor` — MockEvm
  and MockMove, both routing through Bridge::submit. EvmError::Revert
  and MoveError::Abort for VM-flavoured error semantics.
  - 5 unit tests covering happy path, error paths, and EVM/Move
    canonical equivalence on independent states.

- [x] Slice 4 (commit `e008a30`): cross-VM parity property tests in
  `crates/gsxdb-bridge/tests/cross_vm_parity.rs`.
  - `interleaved_evm_move_preserves_invariant` — **THE S3 EXIT GATE**
  - `evm_only_preserves_invariant`, `move_only_preserves_invariant`
  - `evm_and_move_canonical_equivalents_match`
  - **Exit-gate run:** `PROPTEST_CASES=10000 cargo test --release` —
    10,000 cases pass in 0.17s. All four properties at 10k pass in 0.20s.

- [x] Slice 5 (commit `800e61c`): `docs/iq/IQ-2-mock-vms-vs-real-vms.md`
  + `docs/spec/dual-vm-projectors.md`. IQ-2 records the decision to
  ship S3 with mock executors and defer real revm/Move VM to S3.5.

## S3 EXIT GATE: ✅ MET

10,000 cases of interleaved EVM+Move transactions over a seeded
8-address state preserve the dual-projection invariant at every step.
Sub-properties (EVM-only, Move-only, encoding-symmetry) all pass at 10k.

Real-VM integration deferred per IQ-2.

## Next

- **~~S3.5~~ dissolved per IQ-3.** Real-VM integration folds into S5
  (cross-VM intent queue) where it actually has bug-finding value, and
  the Move VM dialect choice is reframed as a launch-readiness
  milestone — not a phase-1 sprint deliverable. See
  `docs/iq/IQ-3-move-vm-choice.md`.
- **S4**: CE-MVCC OCC. Concurrent execution with optimistic concurrency
  control. Builds on the canonical state + bridge gate.

## Blockers

None.

## Open questions / IQ candidates surfaced

- ~~IQ-3: which Move VM crate~~ — **decided 2026-05-08**: defer the
  dialect choice to launch readiness; hand-rolled minimal interpreter
  is the contingency if a sprint forces the question first. See
  `docs/iq/IQ-3-move-vm-choice.md`.
- IQ-4: address-shape mismatch (EVM 20-byte vs Aptos Move 32-byte) —
  becomes urgent when dialect is chosen
- IQ-5: nonce semantics (EVM has them; Move doesn't in the same way) —
  becomes urgent when real Move execution lands
- (Carried from S2) Native int width: `u128` for now; phase-2 may move
  to U256.
