# Sprint S4 state

**Branch:** `state/ce-mvcc-occ`
**Started:** 2026-05-08
**Exit gate:** Block-level `parallel_equals_sequential` — for any block of intents, parallel `BlockExecutor::execute` produces the same final state as sequential `Bridge::submit`. 10k cases in release.

## Plan

S4 broken into four slices:

1. **MvStore** — multi-version balance store sitting in front of canonical State.
2. **Txn + Validator** — read/write set tracking + OCC conflict detection.
3. **BlockExecutor** — Aptos Block-STM driver with rayon parallelism + retry loop.
4. **Property tests + spec doc + closeout.**

## Completed

- [x] Slices 1-3 (commit `a604b4d`): `gsxdb-bridge::occ` with mv_store
  + txn + block_executor. Aptos-style per-tx-index ordering. Iteration
  cap = 2*n+4. Consolidation through `State::apply` with a fresh
  `BridgeToken`.
  - 21 unit tests across the three submodules
  - Added rayon dep

- [x] Slice 4 (this slice): property tests + Bridge self-transfer
  fix + spec doc + closeout.
  - `crates/gsxdb-bridge/tests/block_executor.rs`:
    - `parallel_equals_sequential` — **THE S4 EXIT GATE**
    - `dual_projection_holds_after_block`
    - `total_supply_preserved`
    - `empty_block_is_identity`
  - **Pre-existing Bridge bug surfaced and fixed.** The
    parallel-vs-sequential property minimised to a self-transfer of
    amount 1; `Bridge::submit` was over-crediting because the credit-
    write of `to` overwrote the debit-write of `from` when `from==to`.
    Added a no-op guard + two regression tests.
  - `docs/spec/ce-mvcc-occ.md` — full spec doc.

## S4 EXIT GATE: ✅ MET

10,000 cases of `parallel_equals_sequential` in release pass in 0.54s.
All four block_executor proptests at 10k pass in <1s total. The
dual-projection invariant survives parallel execution (verified by
`dual_projection_holds_after_block`).

## Workspace test count

77 tests pass workspace-wide (was 50):

- gsxdb-state: 33 (unchanged from S3)
- gsxdb-bridge lib: 30 (was 7) — +21 occ unit tests + 2 self-transfer regression tests
- gsxdb-bridge integration tests/block_executor.rs: 4 (new)
- gsxdb-bridge integration tests/cross_vm_parity.rs: 4 (unchanged)
- gsxdb-bridge integration tests/persistent_e2e.rs: 4 (unchanged)
- gsxdb-lane: 2 (unchanged)

## Next

- **S5**: cross-VM intent queue. EVM-shape tx invoking a Move
  resource (or vice versa) atomically. Surfaces real revm and
  potentially the Move VM dialect question (per IQ-3, IQ-4, IQ-5).
- **S6**: Verkle state tree.

## Blockers

None.

## Open questions / IQ candidates surfaced

- Per-storage-slot read granularity for the MV store (becomes urgent
  in S5 when contract storage lands).
- Cross-VM read/write sets that span EVM and Move boundaries (S5).
- MV log persistence for post-mortem replay (S8).
- (Carried) IQ-4 address-shape, IQ-5 nonces — both deferred until real
  Move VM lands.
