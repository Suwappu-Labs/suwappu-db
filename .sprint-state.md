# Sprint S5 state

**Branch:** `bridge/intent-bundles`
**Started:** 2026-05-08
**Exit gate:** Bundle atomicity — for any bundle, a failed step reverts everything; a successful bundle is equivalent to sequential step application. The dual-projection invariant survives. 10k cases.

## Plan

S5 broken into five slices:

1. **Bundle types** — `Bundle`, `BundleStep::{Evm, Move}`, `BundleResult`, `BundleOutcome`.
2. **Standalone bundle executor** — save-and-restore atomicity on `&mut State`.
3. **Contract registry** — address-keyed `BundleGenerator` closures with `CallCtx`.
4. **Block-level integration** — `Intent::Call` variant + `BlockExecutor::execute_with_registry` dispatching calls within OCC.
5. **Property tests + spec doc + closeout.**

## Completed

- [x] Slices 1-4 (commit `e035edb`): `gsxdb-bridge::bundle` with three
  submodules (types, executor, registry) + block-level integration via
  `Intent::Call` and `execute_with_registry`. Per-bundle local
  accumulator for intra-bundle reads so step `n+1` sees step `n`'s
  writes (the MV store deliberately excludes same-idx writes for OCC
  correctness; bundles are atomic at one idx, so they need their own
  read path).
  - 20 new lib tests (16 bundle + 4 Call dispatch)
  - `Intent` lost `Copy` (calldata is `Vec<u8>`)

- [x] Slice 5 (this slice): cross-VM bundle property tests + spec doc.
  - `crates/gsxdb-bridge/tests/cross_vm_bundles.rs`:
    - `bundle_atomicity` — **THE S5 EXIT GATE**
    - `bundle_equivalence_to_sequential`
    - `dual_projection_holds_across_bundles`
    - `total_supply_preserved_across_bundles`
    - `first_step_failure_is_pure_noop`
  - `docs/spec/cross-vm-intent-queue.md` — full spec doc.

## S5 EXIT GATE: ✅ MET

10,000 cases of `bundle_atomicity` in release pass in 0.14s. All five
proptests at 10k pass in <0.2s total. The dual-projection invariant
survives any bundle sequence; total supply is exactly preserved.

## Workspace test count

102 tests pass workspace-wide (was 77):

- gsxdb-state: 33 (unchanged)
- gsxdb-bridge lib: 50 (was 30) — +20 (16 bundle + 4 Call dispatch)
- gsxdb-bridge tests/block_executor.rs: 4 (unchanged)
- gsxdb-bridge tests/cross_vm_bundles.rs: 5 (new)
- gsxdb-bridge tests/cross_vm_parity.rs: 4 (unchanged)
- gsxdb-bridge tests/persistent_e2e.rs: 4 (unchanged)
- gsxdb-lane: 2 (unchanged)

## Next

- **S6**: Verkle state tree. Commit to a Verkle tree of the canonical
  state for stateless validation. Independent of VM choice; pure
  cryptography + tree updates per block.
- **S7**: Anchor log. Cross-chain LTPAnchorRegistry parity.
- **S8**: DAG store + recovery. Durable block storage + replay-from-MV-log.

## Blockers

None.

## Open questions / IQ candidates surfaced

- **Recursive contract calls.** Phase-1 forbids depth > 0. Becomes
  urgent when real revm/Move land — `CallCtx::depth` is the substrate.
- **Storage slot reads/writes.** Bundles only touch the balance map
  in S5. S6/S7 may surface the per-slot question.
- **Persistent intent queue across blocks.** S5 is in-memory only;
  S8 recovery introduces durability.
- (Carried) IQ-4 address-shape, IQ-5 nonces — both deferred until real
  Move VM lands.
