# Docker Compose for suwappudb with op-reth integration
# This demonstrates the full ecosystem setup for local development

version: '3.8'

services:
  # OP Stack L2 node (op-reth)
  op-reth:
    image: us-docker.pkg.dev/oplabs-tools-artifacts/images/op-reth:latest
    ports:
      - "8545:8545"  # JSON-RPC
      - "8546:8546"  # WebSocket
    environment:
      - OP_RETH_DATADIR=/data/op-reth
      - OP_RETH_NETWORK=optimism-sepolia
    volumes:
      - op-reth-data:/data/op-reth
    networks:
      - suwappudb-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8545"]
      interval: 10s
      timeout: 5s
      retries: 5

  # GSX-DB Server
  suwappudb-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8660:8660"  # RPC
      - "9660:9660"  # Metrics
    environment:
      - CONFIG_PATH=/etc/suwappudb/config.toml
      - RUST_LOG=info
    volumes:
      - suwappudb-data:/data/suwappudb
      - ./config.example.toml:/etc/suwappudb/config.toml:ro
    depends_on:
      op-reth:
        condition: service_healthy
    networks:
      - suwappudb-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8660/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  op-reth-data:
  suwappudb-data:

networks:
  suwappudb-net:
    driver: bridge
