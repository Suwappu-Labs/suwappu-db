# Pass B — Security audit + hardening ledger

**Engagement:** Pass B (post Phase-1 launch readiness)
**Started:** 2026-05-16
**Scope:** gsx-db (this repo). Pass B items B1 (gsx-dag side) and B8
(gsx-dag agent files) are out of scope here — they live in the
`GlobalSettlementNetwork/gsx-dag` repo and are tracked separately.

This ledger records the verdict + closing artefact for each item.

## B1 — Workspace lints alignment

**Verdict:** ✅ Already met.

`Cargo.toml` declares:

```toml
[workspace.lints.rust]
unsafe_code = "forbid"

[workspace.lints.clippy]
all = { level = "deny", priority = -1 }
pedantic = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
```

Every workspace crate inherits via `[lints] workspace = true`.
No `unsafe { }` block exists in `crates/**/src/**.rs`; the workspace
lint catches any future regression at compile time.

The gsx-dag side of B1 (mirroring this declaration into
`GlobalSettlementNetwork/gsx-dag/Cargo.toml`) is tracked separately
in that repo.

## B2 — Panic-in-untrusted-input sweep

**Verdict:** ✅ Met for the production surfaces audited.

### Surfaces audited

| File | Pre-B2 hits | Disposition |
|---|---|---|
| `crates/gsxdb-server/src/rpc.rs` | 2 `.unwrap()` calls in `get_balance` / `get_coin_value` | extracted `parse_address_param` helper that returns `Option<Address>`; both call sites switched to `match`. |
| `crates/gsxdb-bridge/src/recovery/store.rs::RedbBlockStore::open` | 4 `.expect()` on `begin_write` / `open_table` / `commit` | switched to `?` with `BlockStoreError::backend` rewrapping; return type widened from `redb::DatabaseError` to `BlockStoreError`. Only test callers, no public API break beyond the error variant. |
| `crates/gsxdb-bridge/src/anchor/credential.rs` | 9 `.expect` hits | all in `#[cfg(test)]` mod tests (line numbers > 500). No production-path panics. |
| `crates/gsxdb-bridge/src/anchor/l1_reader.rs` | 0 | already clean. |
| `crates/gsxdb-bridge/src/sync/l2.rs` | 2 `.expect` hits | both in `#[cfg(test)]`. |
| `crates/gsxdb-bridge/src/anchor/types.rs` | 0 untrusted-input panics | uses `copy_from_slice` on known-32-byte buffers; safe. |

### Tests

5 new unit tests in `rpc.rs` cover the new helper:

- accepts canonical 40-char hex
- accepts `0x`-prefixed
- rejects wrong-length string
- rejects non-hex chars
- rejects non-string JSON values

12 recovery-store tests still pass under the new error type.

### Out of scope (gsx-dag)

The plan also names consensus + transport panic surfaces in gsx-dag
(`crates/gsx-rpc/src/methods.rs`, `crates/gsx-consensus/**`,
`crates/gsx-transport/{scion,gateway}.rs`). Those live in the gsx-dag
repo and are tracked there.

## B3 — CI security gates

**Status:** pending.

`.github/workflows/` additions:

- `cargo-audit.yml` — runs `cargo audit` on push + nightly cron.
- `secret-scan.yml` — gitleaks / trufflehog on every PR.
- `clippy-strict.yml` — `cargo clippy --workspace -- -D warnings`
  (verify existing CI runs this; the workspace lints already
  declare `all = deny` so the lint set is uniform).

## B4 — Consensus-time clock injection

**Status:** pending.

gsx-db has no consensus-time path of its own (consensus runs in
gsx-dag). Audit gsx-db for `SystemTime::now()` in code paths that
must be deterministic across replicas (block-recovery, anchor MAC,
snapshot validation). The known gsx-dag instance
(`gsx-node/events.rs:104`) is out of scope here.

## B5 — Key custody enforcement model

**Status:** pending.

`docs/spec/key-custody.md` is a draft; clarify that HSM-only custody
for Authority validators is enforced operationally (deployment
tooling), not code-level. Cross-reference
`docs/architecture/deployment-topology.md`.

## B6 — Network endpoint auth posture

**Status:** pending.

Add an opt-in bearer-token middleware behind a cargo feature,
default off. Document the firewall-or-ALB requirement explicitly in
`docs/architecture/deployment-topology.md` with sample nginx /
Cloudflare Access configs.

## B7 — Anchor / credential / l1_reader deep-review

**Status:** pending.

Post-S11 deep-review on:

- `crates/gsxdb-bridge/src/anchor/credential.rs`
- `crates/gsxdb-bridge/src/anchor/l1_reader.rs`
- `crates/gsxdb-bridge/src/anchor/signing.rs` (S11.3-new)
- `crates/gsxdb-bridge/src/anchor/dispatcher.rs` (S11.1-revised)
- `crates/gsxdb-bridge/src/anchor/log.rs` (S11.2-revised)
- `contracts/src/LTPAnchorRegistry.sol`

Findings go inline in this ledger.

## B8 — gsx-dag missing subagent `.md` files

Out of scope; tracked in the gsx-dag repo.

---

## Exit criteria (this ledger)

Each item lands a verdict (✅ met / ⚠ accepted with caveat /
❌ blocked) and either a code reference or a follow-up issue.
Pass B closes when every item carries a non-pending status.
